import { meta } from "./meta";
import { userTargets } from "./user-targets";
import { userProfiles, genderEnum, activityLevelEnum, goalEnum } from "./user-profiles";
import { logs } from "./logs";
import { meals } from "./meals";
import { mealItems } from "./meal-items";

export const schema = {
  meta,
  userTargets,
  userProfiles,
  genderEnum,
  activityLevelEnum,
  goalEnum,
  logs,
  meals,
  mealItems,
};
